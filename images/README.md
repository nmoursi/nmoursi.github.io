`nada.jpg` is the headshot shown in the sidebar (800x800, cropped square from IMG_7983.heic).

To replace it, drop in a new square JPG with the same filename. If the file is missing,
the site falls back to a circular "NM" monogram — nothing breaks.
